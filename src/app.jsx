import React from "react";
import ReactDOM from "react-dom";

// Setup React.
const domContainer = document.getElementById('main');
const root = ReactDOM.createRoot(domContainer);

root.render(<h1>Howdy</h1>);